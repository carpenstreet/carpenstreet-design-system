import { Canvas, Controls, Meta } from '@storybook/blocks';
import * as ImageIconStories from './ImageIcon.stories';

<Meta of={ImageIconStories} />

# Image Icon

- width와 height을 이용해 아이콘의 크기를 결정합니다.
- color는 디자인 시스템에 정의된 색상만 사용됩니다.

<Canvas of={ImageIconStories.Default} />

<Controls />
