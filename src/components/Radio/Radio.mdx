import { Canvas, Controls, Meta, Source } from '@storybook/blocks';
import * as RadioStories from './Radio.stories';

<Meta of={RadioStories} />

# Radio

`Input Radio` 기능을 구현한 컴포넌트입니다.

MUI의 `Radio` 컴포넌트를 사용합니다.

<Canvas of={RadioStories.Default} />

<Controls include={['color', 'size', 'disabled']} />

# RadioGroup

MUI의 `RadioGroup` 컴포넌트와 함께 사용하면  
Radio 구성 요소를 그룹화하는데 유용합니다.

필수 사용은 아니며, 필요한 상황에 선택하여 사용 가능합니다.

<Canvas of={RadioStories.RadioGroup} />
<Controls of={RadioStories.RadioGroup} include={['color', 'size', 'disabled']} />
