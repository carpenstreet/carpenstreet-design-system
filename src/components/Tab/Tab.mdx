import { Canvas, Controls, Meta } from '@storybook/blocks';
import * as TabStories from './Tab.stories';

<Meta of={TabStories} />

# Tab

GNB 등에서 쓰이는 `Tab` UI 컴포넌트입니다.

MUI의 `Tabs` 컴포넌트로 감싸서 사용합니다. 자세한 사용법은 [MUI Tabs](https://mui.com/material-ui/react-tabs/)를 참고해주세요.

<Canvas of={TabStories.Default} />

<Controls incldue={['label', 'size', 'disabled', 'sx']} exclude={['ref', 'component']} />
