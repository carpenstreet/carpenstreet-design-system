import { Canvas, Controls, Meta } from '@storybook/blocks';
import * as AconLogoStories from './AconLogo.stories';

<Meta of={AconLogoStories} />

# Acon

배경색(color/gray/400)은 포함되지 않습니다

<Canvas of={AconLogoStories.Default} />

<Controls incldue={['type', 'width', 'height']} />
