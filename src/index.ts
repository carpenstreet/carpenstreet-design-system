import {Button, Letter, Shadow, Icon} from "./components";
import Color from "@shared/settings/Color.ts";

const Components = {
  Button,
  Letter,
  Shadow,
  Icon,
}

const CDS = {
  Components,
  Color
}

export default CDS;